# 📧 Homework 6 for QAGURU 23

## Задание: Система рассылки писем

---

## 📋 Часть A. Функции

Создайте набор функций для обработки информации о письме. Каждая функция должна выполнять **ровно одно действие** и возвращать результат.

### ⚙️ Функции для реализации:

| № | Функция | Описание |
|---|---------|----------|
| 1 | `normalize_addresses()` | Приводит адреса к нижнему регистру и убирает пробелы |
| 2 | `add_short_body()` | Создает короткую версию тела (первые 10 символов + "...") |
| 3 | `clean_body_text()` | Заменяет табы и переводы строк на пробелы |
| 4 | `build_sent_text()` | Создает форматированный текст письма |
| 5 | `check_empty_fields()` | Проверяет, заполнены ли обязательные поля |
| 6 | `mask_sender_email()` | Создает маскированную версию email (первые 2 символа + "***@" + домен) |
| 7 | `get_correct_email()` | Проверяет наличие @ и допустимые домены (.com, .ru, .net) |
| 8 | `create_email()` | Создает базовую структуру письма |
| 9 | `add_send_date()` | Добавляет текущую дату в формате YYYY-MM-DD |
| 10 | `extract_login_domain()` | Разделяет email на логин и домен |

---

## 📤 Часть B. Отправка письма

Создать функцию `sender_email()` для отправки письма с валидацией и обработкой.

### 📥 Параметры функции:

```python
def sender_email(
    recipient_list: list[str],
    subject: str,
    message: str,
    *,
    sender="default@study.com"
) -> list[dict]:
    ...
```

### 🔄 Последовательность обработки:

1. ✅ Проверить, что список получателей не пустой
2. ✅ Проверить корректность email адресов
3. ✅ Проверить заполненность темы и тела письма
4. 🚫 Исключить отправку самому себе
5. 🔧 Нормализовать все текстовые данные
6. 📝 Создать письмо для каждого получателя
7. 📅 Добавить дату отправки
8. 🎭 Замаскировать email отправителя
9. ✂️ Создать короткую версию тела письма
10. 📄 Сформировать итоговый текст письма

### 📦 Структура результата:

```python
[
    {
        'recipient': 'admin@company.ru',
        'sender': 'default@study.com',
        'subject': 'Hello!',
        'date': '2026-01-01',
        'body': 'Привет, коллега!',
        'masked_sender': 'de***@study.com',
        'short_body': 'Привет, ко...',
        'sent_text': 'Кому: admin@company.ru, от default@study.com\nТема: Hello!, дата 2026-01-01\nПривет, ко...'
    },
    ...
]
```

### 🎯 Результат:

Список с готовыми письмами, содержащими все обработанные данные.

---

## 📚 Пример использования:

```python
recipients = ["admin@company.ru", "test@gmail.com", " USER@Service.NET "]
emails = sender_email(
    recipient_list=recipients,
    subject="Quarterly Report",
    message="Hello,\n\tHere is the report.",
    sender="default@study.com"
)

for email in emails:
    print(email['sent_text'])
```
